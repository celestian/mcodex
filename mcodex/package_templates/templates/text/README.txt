This directory contains the authoritative sources for the text.

- text.md: the Markdown source
- metadata.yaml: frozen metadata
- .snapshot/: versioned snapshots created by mcodex

Build outputs go to the repository-level `artifacts/` directory.

